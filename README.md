# Form follows f(x)

A Level 1 World in the Cabinet of Curiosities network — a home for creative
coding work: p5.js sketches, algorithmic studies, generative art, and
interactive web experiments. Live at `fffx.cabinetofcuriosities.in`.

## Philosophy

The name is a play on "form follows function." Here, form follows *f(x)* —
the shapes on the page are the visible output of a function, not a
container something was poured into afterward. The landing page takes this
literally: there is no card grid or portfolio layout. The page itself is
generated by recursively subdividing a rectangle, the same kind of process
(seeded, recursive, geometric) that the projects linked from it are made
of. The medium of the index page is the same as the medium of the work it
indexes.

This is a sibling site to **The Bookshelf of Curiosities**, sharing its
MkDocs + custom-landing-page architecture (see `LANDING-PAGE-NOTES.md`),
but with its own visual language — no gallery-wall metaphor, no serif/sepia
palette, no ghost-letter system. fffx's metaphor is computational: grid
paper, monospace coordinates, depth labels, a field of rectangles. See
`DESIGN-SYSTEM.md` for the full visual spec.

## Repository structure

```text
form-follows-fx/
├── mkdocs.yml              MkDocs Material config for the rest of the site
├── requirements.txt        Python deps for `mkdocs build` / `mkdocs serve`
├── README.md               This file
├── DESIGN-SYSTEM.md         Visual language / interaction rules
├── LANDING-PAGE-NOTES.md    Implementation architecture, rendering pipeline
└── docs/                    MkDocs docs_dir
    ├── index.html           Standalone landing page (NOT index.md — see notes)
    ├── assets/                    All landing-page CSS/JS lives here, kept out of the
    │   │                          section folders below so nothing collides with them
    │   ├── css/
    │   │   └── landing.css        All landing-page CSS, scoped under .fffx-landing
    │   └── js/
    │       ├── data.js            Source of truth: landingConfig + sections[] + entries[] (the portal IA)
    │       ├── random.js          Seeded PRNG + deterministic filler-variant picker
    │       ├── subdivision.js     Rectangle tree, candidate filtering, scoring, assignment (no DOM)
    │       └── layout.js          DOM rendering, resize handling — orchestrates the above
    ├── images/CirclePacking/      Images for the Circle Packing writeup
    ├── recreating-the-past/
    │   └── vera-molnar.md         Vera Molnar study writeup — real content
    ├── tools-and-libraries/
    │   ├── circle-packing-library.md   Circle Packing Library writeup — real content
    │   ├── mandala-generator.md        Placeholder
    │   ├── lenticular-image-generator.md  Placeholder
    │   └── harmonics-dance-of-planets.md  Placeholder
    ├── prompt-collections/genuary.md            Placeholder
    ├── deep-studies/100-gradients.md            Placeholder
    ├── deep-studies/particle-systems.md         Placeholder
    ├── generative-projects/windows-of-berlin.md Placeholder
    ├── image-experiments/image-filters.md       Placeholder
    ├── sketch-families/flow-fields.md           Placeholder
    ├── sketch-families/perlin-noise.md          Placeholder
    ├── physical-outputs/plotter-work.md         Placeholder
    ├── physical-outputs/code-to-fabrication.md  Placeholder
    └── archives/legacy-processing-archive.md    Placeholder, do-not-touch-yet
```

- `docs/index.html` is a complete, standalone HTML document. It is **not**
  rendered through Material's theme — MkDocs copies it through byte-for-byte
  as the site's homepage. This is deliberate: it sidesteps the
  header/sidebar-hiding workarounds the Bookshelf project needed, because
  there's no Material chrome on this page to hide in the first place.
- Every other page under `docs/` is normal MkDocs Material content with
  the standard sidebar/nav — but `mkdocs.yml`'s `nav:` currently lists only
  the two pages with real, finished-enough content (Vera Molnar, Circle
  Packing Library). The thirteen placeholder pages exist on disk, still
  get built, and are still linked from the landing page itself (muted —
  see `status` below) — they're just commented out of the nav so they
  don't bury the few pages worth reading. Uncomment a page's nav line once
  it has real content.
- `docs/assets/js/data.js` is the single source of truth for the landing
  page's information architecture — every portal's title, href, taxonomy,
  and importance lives there as one entry in `entries[]`. Editing the
  landing page's content means editing this file, not the HTML or the
  layout engine. See "Adding a project" below for the full field
  reference.

## Adding a portal to the landing page

Each entry in `docs/assets/js/data.js`'s `entries[]` is a **portal** — a
collection, study, tool, project, or archive grouping. Never add one entry
per tiny sketch version; group related sketches under one portal and link
out from its write-up page instead. The data decides what exists and how
important it is; the subdivision algorithm in `subdivision.js` decides
where it lives visually — these are deliberately separate concerns.

Required fields: `id`, `title`, `subtitle`, `href`, `section`, `kind`,
`order`, `weight`, `status`, `tags`, `era`, `location`. Optional: `image`,
`years`, `repo`, `relatedLinks`, `sourceFolder`, `notes`, `children`,
`themeTags`.

- `order` controls placement priority (lower = placed first, gets first
  pick of well-fitting rectangles).
- `weight` is `1`–`4`: `1` archive/secondary, `2` regular, `3` important
  collection/study, `4` major feature portal. Controls the target
  rectangle area the layout engine scores candidates against — see
  `LANDING-PAGE-NOTES.md` for the scoring formula.
- `status` is one field doing one job — both the visibility switch and the
  "is this actually finished" flag: `true` renders normally, `false` is
  excluded from the field entirely, `"wip"` renders but muted (dashed
  border, lower opacity, a `wip` tag in the meta line). This is how a real
  portal can exist and be linkable on day one without looking finished
  before it is.
- `era` (`current-web` / `p5-archive` / `processing-legacy` / `other-code`)
  records where the underlying sketch/tool actually lives, independent of
  how the portal currently renders.
- If the portal has its own write-up page, add the `.md` file under the
  matching `docs/<section-folder>/` and reference it in `mkdocs.yml`'s
  `nav:` — but only once it has real content. Placeholder pages
  intentionally stay out of the MkDocs nav (commented out, not deleted)
  so they don't bury the few pages worth reading; they're still reachable
  from the landing page itself via their `href`.

## Status

Active development. Fifteen portal entries exist in `docs/assets/js/data.js`,
covering all major sections (prompt collections, deep studies, recreating
the past, tools & libraries, generative projects, image experiments,
sketch families, physical outputs, archives). Two have real, finished-enough
write-ups and are listed in `mkdocs.yml`'s `nav:`: **Vera Molnar**
(`recreating-the-past/vera-molnar.md`) and **Circle Packing Library**
(`tools-and-libraries/circle-packing-library.md`, weight 4, feature tile,
`p5-circle-packing` repo). The other thirteen are intentionally minimal
placeholder pages — title, status, source material, a note that content
is pending — and render muted on the landing page via `status: "wip"`.
**Legacy Processing Archive** is one of them: acknowledged on the landing
page, but its underlying `.pde` source files are explicitly not being
touched or migrated yet.

## Changelog

- **2026-06-28** — Initial recursive-subdivision landing page built
  (`index.html`, `css/style.css`, `js/data.js`, `js/layout.js`). Fixed a
  latent bug where the mobile candidate-rect filter referenced an undefined
  config key (`minThumbWidthMobileRatio`), silently producing zero valid
  rectangles on narrow viewports. Removed a stray `extra_css` reference to
  a Bookshelf stylesheet that was never part of this repo. Files moved from
  a flat `docs/` into `docs/css/` and `docs/js/`. Nav slugs settled on
  `PackingShapes.md` / `VeraMolnarRetrospective.md`.
- **2026-06-29** — Rebuilt the layout engine to spec: split into
  `js/random.js` (seeded PRNG, extracted standalone) and `js/subdivision.js`
  (pure tree-building/scoring/assignment, no DOM) so `js/layout.js` is
  rendering-only. `css/style.css` renamed to `css/landing.css`. Switched
  `weight` from an ad hoc 2/3 scale to the spec's 1–4 (small/medium/
  large/feature) scale and rebalanced existing entries across all four
  tiers. Rectangle ids now encode their path from the root
  (`"0"`, `"0.0"`, `"0.1"`, ...), which turns ancestor/descendant blocking
  into a string-prefix check instead of a parent-pointer walk. Added a
  position-based scoring tiebreaker (earlier-ordered entries drift toward
  the upper-left), a real geometric-overlap check alongside the
  ancestor/descendant check when blocking rects after each assignment, and
  filler-cell visual variety (hatch, dots, gradient wash, glyphs, code
  fragments, coordinate labels — see `DESIGN-SYSTEM.md`). Filler now draws
  only from leaf rects (a clean tiling with no overdraw) instead of every
  unblocked depth. Added a hover-only "live re-subdivision" hint (two
  hairlines growing from a tile's center) and a tag-reveal-on-hover row to
  tiles, an obvious `:focus-visible` outline, a very slow background-grid
  shimmer (disabled under `prefers-reduced-motion`), and a quieter-filler
  rule on mobile.
- **2026-06-29** — Removed the generic decorative background grid (it had
  no relationship to the actual subdivision data, which is why tiles never
  looked like they belonged to it) and replaced it with a real structure
  layer: every rect in the tree (not just leaves/tiles) renders as an
  inset, low-alpha div, painted parent-before-child, so gaps at every
  split reveal the parent's tint and deeper nesting visibly darkens
  through stacked alpha. New config: `landingConfig.layout.structInset`,
  `structAlpha`.
- **2026-06-29** — Set up the full fffx information architecture: moved
  `docs/PackingShapes.md` and `docs/VeraMolnarRetrospective.md` into
  section-based subfolders as `tools-and-libraries/circle-packing-library.md`
  and `recreating-the-past/vera-molnar.md`, and added thirteen new
  placeholder portal pages across `prompt-collections/`, `deep-studies/`,
  `generative-projects/`, `image-experiments/`, `sketch-families/`,
  `physical-outputs/`, and `archives/`. Expanded every `data.js` entry to
  the full required schema (`era`, `migrationStatus`, `location`, plus
  `sourceFolder`/`relatedLinks`/`notes` where relevant) and added
  `landingConfig.layout.mutedMigrationStatuses` plus a
  `.fffx-tile--muted` CSS treatment so placeholder portals render as real,
  clickable, but visibly de-emphasized tiles instead of either looking
  finished or being hidden outright. Commented out (not deleted) the nav
  entries for all thirteen placeholders in `mkdocs.yml`, leaving only Vera
  Molnar and Circle Packing Library — the two pages with real
  content — in the MkDocs sidebar, so the sidebar doesn't bury them among
  empty stubs. Placeholder pages are still built and still linked directly
  from the landing page.
- **2026-06-29** — The layout now reseeds every page load (was: a fixed
  seed string, so the subdivision tree never changed between reloads) —
  `order`/`weight` still drive assignment identically every load. Also
  added per-section accent colours (a tile's `.fffx-tile-meta` text and a
  new corner "tab" mark now pick up one of ten muted hues based on its
  `section`) and a warm/cool tint split on the structure layer, so the
  recursive structure itself reads as a colour map rather than only a
  darkness gradient. Full detail in `LANDING-PAGE-NOTES.md` and
  `DESIGN-SYSTEM.md`.
- **2026-06-29** — `order` now also controls *where on the page* an entry
  lands, not just assignment priority: candidates are sorted into reading
  order (left to right, then top to bottom by row) and each entry can
  only be assigned a rect near its proportional position in that order.
  So entry 0 reliably lands near the top-left, the last entry near the
  bottom-right, and everyone in between stays roughly in the same spot
  relative to each other across reloads — even though the rect *shapes*
  keep reseeding. `weight` still controls size within whatever window an
  entry lands in.
- **2026-06-29** — Fixed two issues flagged after the colour pass: the
  structure layer's warm/cool tint was cancelling out to flat neutral
  grey-brown almost everywhere (now keyed off the top-level split branch,
  so it forms two coherent colour zones instead); and tiles were covering
  far less of the field than intended (~18% measured, vs. a ~65% target)
  because tile size was scored against a fixed px² constant unrelated to
  the actual field size or entry count. Tiles now target
  `targetTileAreaFraction` (~65%) of the field in aggregate, computed
  fresh per render from the live field size and entry weights. Desktop
  subdivision also goes one level deeper so the smaller remaining filler
  space still reads as fine texture rather than a few large leftover
  chunks. Full detail in `LANDING-PAGE-NOTES.md` and `DESIGN-SYSTEM.md`.
- **2026-06-29** — Moved `docs/css/` and `docs/js/` into `docs/assets/css/`
  and `docs/assets/js/` (user-initiated, to keep the landing page's own
  CSS/JS out of the way of the section folders sitting alongside them at
  `docs/` root). Updated `index.html`'s `<link>`/`<script src>` paths
  accordingly; the JS files' own relative imports (`./data.js`, etc.) were
  untouched since all four moved together. No other file references the
  old `docs/css/`/`docs/js/` paths going forward — see `LANDING-PAGE-NOTES.md`.
- **2026-06-29** — Collapsed `status`/`migrationStatus` (two fields, two
  jobs — one a hard show/hide switch the renderer read directly, one
  separate content-state bookkeeping that only drove the muted-tile
  style) into a single `status` field: `true` (renders normally), `false`
  (excluded entirely), `"wip"` (renders, muted). `migrationStatus` and
  `landingConfig.layout.mutedMigrationStatuses` are gone. All thirteen
  placeholder entries are now `status: "wip"`; Vera Molnar and Circle
  Packing Library (the two with real content) are `status: true`. None
  currently use `status: false` — nothing on the site needs to be fully
  hidden yet, just muted.
- **2026-06-29** — Fixed two issues flagged by inspection. (1) Removed
  the dashed border from muted (`status: "wip"`) tiles — de-emphasis is
  now signalled by line weight only (`1px` at rest / `2px` on hover vs a
  live tile's `1.5px`/`2.5px`), never dashing. (2) The structure layer's
  per-rect inset was flat (every rect inset the same amount from its own
  bounds), which meant a child and its parent — sharing 3 of 4 edges —
  ended up with coinciding inset edges along those shared sides; the
  "nested rectangles" were only ever visible at the newest split seam,
  reading as borders that were "in touch." Inset is now cumulative by
  depth (`rect.depth × structInset`), so every generation is visibly
  smaller than the one before it on every edge. (3) Tile title/subtitle
  font sizes were set with a viewport-relative `clamp(…vw…)`, so a small
  tile and a large tile got the same size and small tiles' text silently
  overflowed past `overflow: hidden`. Font sizes are now computed per-tile
  from that tile's own rect dimensions, with a 2-line clamp as a safety
  net and the tags row hidden entirely below 200×150px.
- **2026-06-29** — The previous "cumulative by depth" inset fix (entry
  above) still wasn't right — the user correctly diagnosed it as
  computing inset against each rect's *raw*, never-touched bounds as a
  separate rendering-only pass, instead of baking it into the actual
  subdivision. Since raw sibling/parent bounds always touch exactly with
  zero gap, an inset computed independently per node that way has no
  reliable relationship to where the parent is actually drawn — hence
  "haywire." Fixed properly per the user's algorithm: `buildRectTree()`
  in `subdivision.js` now insets each freshly-split child *before*
  pushing it into the tree and recursing — `x += d, y += d, width -= d,
  height -= d` (not `-= 2d`; only the top-left corner moves, the
  bottom/right edge stays on the split line) — so the *next* split
  operates on the already-inset rect. `renderStruct()` in `layout.js`
  no longer does any inset math at all; it just draws the geometry as
  given, because the geometry is now correct at the source.
- **2026-06-29** — Fixed all sixteen `href` values in `data.js` (every
  entry plus one `relatedLinks` href) — they were root-absolute
  (`/recreating-the-past/vera-molnar/`), which only resolves correctly
  when served from a domain root. GitHub Pages currently serves this
  site from `jesmehta.github.io/form-follows-fx/`, one path segment
  deeper, so every link 404'd in production while working under
  `mkdocs serve`. Stripped the leading slash from every href so they
  resolve relative to wherever `index.html` actually sits.
- **2026-06-29** — v1.0 held as a stable milestone; v2.0 work started on
  a local `v2-dark-theme` branch (`main` untouched). v2.0 phase 1–3:
  added `sections[]` to `data.js` (the section registry — id, label,
  order, enabled), and reworked the subdivision tree so the root
  partitions into one contiguous region per enabled section — sized by
  that section's total visible-entry weight, via a linear chain of
  splits rather than a balanced binary tree — *before* any of the usual
  recursive subdivision happens. Every rect now carries a `sectionId`,
  and entry assignment runs once per section, scoped to that section's
  own rects only. A section's tiles now occupy one real, stable area of
  the page rather than being scattered wherever a good-fitting rectangle
  happened to exist. Two follow-up fixes from user inspection: the
  structure-layer inset (`insetRect()`) was still touching parent edges
  on the un-split dimension — fixed by subtracting `2d` from width/height
  instead of `d`; and the tile hover "live re-subdivision hint" changed
  from two lines crossing at center to four offset from the tile's own
  right/top edges. Display/mono fonts and a section-jump menu row are
  still pending — see `LANDING-PAGE-NOTES.md`/`DESIGN-SYSTEM.md`.
- **2026-06-29** — v2.0 phase 4: full dark + cyan colour pass. Every
  `landing.css` colour token flipped from v1.0's light paper-and-ink
  palette to a dark slate ground, with `--accent` narrowed to the one
  saturated colour on the page (cyan, used only for focus/hover, never a
  section's own colour). The ten `--accent-<section>` hues were redone at
  matched saturation/lightness, spaced evenly around the colour wheel.
  The structure layer now tints by each rect's actual section (looked up
  via `SECTION_TINTS` in `layout.js`) instead of the interim warm/cool
  placeholder from phase 1–3. See `DESIGN-SYSTEM.md` for the full token
  table. Section-jump menu row and the display/mono font swap are still
  pending.
- **2026-06-29** — Fixed a real bug the user caught: `vera-molnar` (the
  only entry in `recreating-the-past`) never rendered as a tile. Not a
  weight or order problem — the section-chain partition could slice a
  low-weight section into a sliver narrower than `minThumbWidth` despite
  its area *share* of the field being perfectly reasonable, because the
  chain kept splitting whichever axis was "currently longer" several
  times in a row on a landscape field. Fixed two ways: the chain split
  now picks whichever axis keeps each peeled section closer to square
  (`splitRectSquarified()`), and `layout.js` now computes a hard minimum
  field area guaranteeing every section's share clears its own
  per-entry floor, growing field height (never width) when needed —
  replacing an old mobile-only height heuristic that had no actual
  relationship to thumb-size thresholds. See `LANDING-PAGE-NOTES.md`'s
  "Section minimum-area guarantee" and bug writeup for the full
  mechanism and the math.
- **2026-06-29** — v2.0 phases 5–6: section-jump menu (scrolls to each section's real on-page region) and Space Grotesk + IBM Plex Mono typography, replacing system-sans/Courier New. See `LANDING-PAGE-NOTES.md`.
- **2026-06-29** — Extended v2.0's dark + cyan palette and typography to the rest of the site (every Material-rendered page, not just the landing page). Extracted shared values into `docs/assets/css/tokens.css` (single source of truth: `--fffx-bg`, `--fffx-accent`, `--fffx-font-display`, etc.) — `landing.css` now reads from it instead of hardcoding the same hex values twice, and a new `docs/stylesheets/fffx-material.css` maps the same tokens onto Material's `--md-*` variables. `mkdocs.yml`: `theme.palette` switched to `scheme: slate` / `primary: black` / `accent: cyan` (was a light scheme with an unwired `primary: custom`), `theme.font` switched to Space Grotesk + IBM Plex Mono (was Ubuntu/Ubuntu Mono), added `extra_css` listing both new stylesheets. Fixed a typo in `site_description` while in there. `theme.icon.logo` (a Bookshelf-era tortoise icon) and `theme.favicon` (points at a file that doesn't exist in `docs/images/`) are still open — flagged, not fixed.
